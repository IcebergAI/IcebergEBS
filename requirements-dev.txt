# Development / CI tooling. Installs the runtime + test deps, plus the static
# analysis tools the CI workflow enforces (see .github/workflows/ci.yml).
-r requirements.txt
# Sync Postgres driver for the Alembic CLI / sync-engine paths (tests/test_migrations.py
# and manual `alembic` invocations). Production migrates via the async connection at
# startup and does not need this; operators running the CLI by hand should install it.
psycopg2-binary
ruff
mypy
bandit[toml]
pip-audit
