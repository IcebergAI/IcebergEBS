.env
*.db
venv/
# The builder stage produces /app/.venv; a host .venv copied in by `COPY . .`
# would shadow it with the wrong (dev-including, host-platform) interpreter.
.venv/
__pycache__/
.git/
tests/
*.pyc
nginx/certs/
DEPLOYMENT.md
