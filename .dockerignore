.env
*.db
venv/
# The image's venv lives at /opt/venv (UV_PROJECT_ENVIRONMENT in the Dockerfile);
# a host .venv swept in by `COPY . .` would bake a wrong (dev-including,
# host-platform) interpreter tree into the image.
.venv/
__pycache__/
.git/
tests/
*.pyc
nginx/certs/
DEPLOYMENT.md
