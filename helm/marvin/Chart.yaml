apiVersion: v2
name: marvin
description: Extension risk monitor — tracks browser and editor extensions and scores them for risk
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: postgresql
    version: "~15.x.x"
    repository: https://charts.bitnami.com/bitnami
