[pytest]
asyncio_mode = auto
# The test DB is a single session-scoped Postgres engine (see tests/conftest.py),
# so fixtures and tests must share one event loop — otherwise asyncpg connections
# raise "attached to a different loop".
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
